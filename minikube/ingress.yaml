apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: appingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ural-aggregator
                port:
                  number: 10000